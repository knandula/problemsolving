/*#include<bits/stdc++.h>
#include<iostream>
#include<stdio.h>

using namespace std;

int main()
{
    while(1)
    {
        int a,b,c;
        scanf("%d %d %d",&a,&b,&c);
        if(a == 0 && b == 0 && c== 0)
            break;

       if((b - a) == (c - b))
       {
        printf("AP %d",(c+(c - b)));
       }
       else if((b/a) == (c/b))
       {
        printf("GP %d",(c*(c / b)));
       }
       printf("\n");
    }
    return 0;
}
*/
